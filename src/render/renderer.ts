// Manages the ECharts instance lifecycle for a single chart block.
// Responsibilities: init, setOption, resize (wired to Obsidian workspace events), dispose.
// Receives a fully-formed ChartOption and renders it — unaware of how the option was built.
